# CRUD
     - install express
     - initialized git
     - install -g nodemon
     - install mongoose
     

# Dependencies 




# Feature
   - server created with the database connection
   - for creating the mini application we use the expressRouter()